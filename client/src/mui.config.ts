import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#6631F7',
        },
        secondary: {
            main: '#E33E7F'
        }
    },
});

export default theme;